# Base URL of the backend (NOT the /api/interview path — the client appends that).
# The backend defaults to :8080 locally (application.yml: ${PORT:8080}),
# so no override is needed for local dev. Set this on Vercel to the
# deployed backend URL.
VITE_API_URL=http://localhost:8080
